You can specify which maps are enabled/disabled by using a plugin like Map-Config and setting css_enable_teleport_ang_fix to false

I'd recommend also putting it to true in gamemode_casual_server.cfg or vice verso